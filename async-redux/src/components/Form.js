import React from "react";
// import { connect } from "react-redux";

const Form = props => {
    return (
        <div>This is the form component
            <button>💩 New?</button>
        </div>
    )
}

export default Form